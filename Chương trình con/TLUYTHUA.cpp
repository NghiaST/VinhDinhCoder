#include <bits/stdc++.h>
using namespace std;
long long a,b,c,d,e,f,g,h;
int main()
{
    cin >> a >> b >> c >> d >> e >> f >> g >> h;
    cout << (long long) (pow(a, e) + pow(b,f) + pow(c,g) + pow(d, h));
}
    
