#include <iostream>
using namespace std;
int main()
{
	long long a;
	cin >> a;
	if (a > 0) cout << a*a*a << ' '; else cout << a*a << ' ';
	cin >> a;
	if (a > 0) cout << a*a*a << ' '; else cout << a*a << ' ';
	cin >> a;
	if (a > 0) cout << a*a*a << ' '; else cout << a*a << ' ';
}
